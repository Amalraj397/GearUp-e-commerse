
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AutoMinima - Premium Diecast</title>
    <link rel="stylesheet" href="/User/assets/css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
<style>
html {
  scroll-behavior: smooth;
}
</style>
</head>
<body>
    <header class="header">
        <nav class="navbar">
          <div class="logo">
            <!-- <h1>Gear-UP</h1> -->
             <img src="/User/assets/images/GEAR-UP/autominima logo.png" alt="gearup logo" class="logo-img">
            <!-- <div class="logo-underli"></div> -->
          </div>
      
          <ul class="nav-links">
            <li><a href="/" class="nav-link" >HOME </a></li>
            <li><a href="#" class="nav-link">Offer-Zone</a></li>
            <li><a href="/shopPage" class="nav-link">Garage</a></li>
            <li><a href="/brandPage" class="nav-link">Manufacturers</a></li>
            <li><a href="/#contactSection" class="nav-link contact-link">Pit Stop</a></li> 
            <li><a href="/aboutUs" class="nav-link">Engine Room</a></li>
          </ul>
      
          <ul class="auth-buttons">
            <% if (user) { %>
                <% console.log("user data:::", user); %>
              <li class="nav-link"  id="user-name">Hi, <%= user.name %></li>
             <div class="dropdown">
              
              <!-- User Icon -->
               <div class="dropdown-user">
                 <button class="dropdown-btn">
                   <i class="fas fa-user" style="font-size: larger;"></i>
                  </button>
                  <div class="dropdown-content">
                    <a href="/userDashboard"><i class="fas fa-user"></i>Account</a>
                    <a href="/myOrders"><i class="fas fa-shopping-bag"></i>My Orders</a>
                    <a href="/wallet"><i class="fas fa-wallet"></i> Wallet</a>
                    <a href="/referandearn"><i class="fas fa-gift"></i> Earn Rewards</a>
                    <a href="/logout" class="logoutBtn"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
                    
                  </div>
                </div>

              <!-- Wishlist Icon -->
              <button class="dropdown-btn-heart" onclick="window.location.href='/Wishlist'">
                 <i class="fas fa-heart"></i><span class="wishlist-badge"><%= wishlistCount ||0 %></span>
              </button>
              
              <!-- Cart Icon -->
              <button class="dropdown-btn-shop" onclick="window.location.href='/userCart'">
                 <i class="fas fa-shopping-cart"></i><span class="cart-badge"><%= cartCount ||0 %></span>
              </button>

              <div class="dropdown-content">
                <a href="/userDashboard"><i class="fas fa-user"></i>Account</a>
                <a href="/myOrders"><i class="fas fa-shopping-bag"></i>My Orders</a>
                <a href="#"><i class="fas fa-map-marker-alt"></i> Your Address</a>
                <a href="#"><i class="fas fa-cog"></i> Settings</a>
                <a href="#"><i class="fas fa-headset"></i> Contact Us</a>
                <a href="/logout"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
              </div>
            </div>

            <% } else { %>
              <li><a href="/login" class="nav-link">Login</a></li>
              <li><a href="/signup" class="nav-link">Sign-up</a></li>
            <% } %>
            </ul>
          </div>
        </nav>
      </header>
      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  document.querySelectorAll(".logoutBtn").forEach((btn) => {
    btn.addEventListener("click", function (e) {
      e.preventDefault(); // stop immediate logout

      Swal.fire({
        title: "Are you sure?",
        text: "You will be logged out of your account.",
        icon: "warning",
        showCancelButton: true,
        confirmButtonColor: "#d33",
        cancelButtonColor: "#3085d6",
        confirmButtonText: "Yes, logout",
        cancelButtonText: "Cancel"
      }).then((result) => {
        if (result.isConfirmed) {
          window.location.href = btn.getAttribute("href"); 
          // uses the original href="/logout"
        }
      });
    });
  });


setInterval(async () => {
  try {
    const response = await fetch('/checkUserStatus');
    if (!response.ok) return; // user not logged in, etc.

    const data = await response.json();
    if (data.isBlocked) {
      Swal.fire({
        icon: 'warning',
        title: 'Account Blocked',
        text: 'Your account has been temporarily blocked. Please contact the admin team.',
        confirmButtonText: 'OK',
        allowOutsideClick: false
      }).then(() => {
        window.location.href = '/logout'; // or custom logout handler
      });
    }
  } catch (err) {
    console.error('Error checking user status:', err);
  }
}, 10000); // 5 seconds

</script>

      
